# Docker-specific environment variables
# This file contains Docker-specific defaults that can be overridden

# Container settings
COMPOSE_PROJECT_NAME=resume-matcher
COMPOSE_FILE=deployment/docker/docker-compose.yml

# Build settings
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# Registry settings (uncomment and configure for production)
# REGISTRY_URL=your-registry.com
# IMAGE_NAME=resume-job-matcher
# IMAGE_TAG=latest

# Resource limits (uncomment to enable)
# API_CPU_LIMIT=1.0
# API_MEMORY_LIMIT=1g
# WORKER_CPU_LIMIT=2.0
# WORKER_MEMORY_LIMIT=2g

# Monitoring settings
# FLOWER_USER=admin
# FLOWER_PASSWORD=change-this-password

# Redis settings for production
# REDIS_PASSWORD=secure-redis-password

# PostgreSQL settings for development
POSTGRES_DB=resume_matcher_dev
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password